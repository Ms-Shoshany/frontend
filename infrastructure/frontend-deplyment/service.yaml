apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: frontend
    service: frontend
spec:
  selector:
    app: frontend
    service: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP